# 마이인보이스 — 2026-04-09 (내일) 할 일

## 우선순위 1: 환경 설정 마무리 (10분)

- [ ] **Supabase Auth Redirect URL 설정**
  - Supabase Dashboard → Authentication → URL Configuration
  - Site URL: `https://myinvoice-mu.vercel.app`
  - Redirect URLs 추가:
    - `https://myinvoice-mu.vercel.app/**`
    - `http://localhost:5173/**`
- [ ] **Google Cloud Console 확인**
  - 승인된 리디렉션 URI에 `https://kpizzslepjubluvetess.supabase.co/auth/v1/callback` 있는지 확인
- [ ] **Vercel 배포 URL에서 로그인 → 워크스페이스 생성 → 대시보드 정상 동작 확인**

---

## 우선순위 2: Supabase SQL 실행 확인 (5분)

- [ ] **003_categories.sql** 실행 여부 확인
  - Supabase Table Editor에서 `categories` 테이블이 보이는지 확인
  - 없으면 SQL Editor에서 003_categories.sql 실행

---

## 우선순위 3: Phase 2 — 팀 협업 기능 구현

### 3-1. DB 스키마 추가
- [ ] `invitations` 테이블 생성 (초대 토큰, 만료일, 수락 여부)
- [ ] workspace_members에 초대 관련 컬럼/정책 보강

### 3-2. 멤버 초대 기능
- [ ] 초대 링크 생성 (토큰 기반, 만료 7일)
- [ ] 초대 수락 페이지 (`/invite/:token`)
  - 로그인 안 되어있으면 → Google 로그인 → 자동 합류
  - 로그인 되어있으면 → 바로 합류
- [ ] 초대 링크 복사 버튼 (이메일 발송은 Phase 3)

### 3-3. 멤버 관리 페이지
- [ ] `/settings/members` 또는 설정 페이지에 멤버 탭 추가
- [ ] 멤버 목록 (이름, 이메일, 역할, 가입일)
- [ ] 역할 변경 (owner만 가능): admin ↔ member
- [ ] 멤버 제거 (owner만 가능)
- [ ] 대기 중인 초대 목록 + 초대 취소

### 3-4. 역할 권한 분기
- [ ] 권한 체크 유틸 함수: `canEdit()`, `canManageMembers()`, `isOwner()`
- [ ] UI에서 권한에 따라 버튼 숨김/비활성화
  - owner: 모든 기능
  - admin: CRUD + 멤버 초대 (역할 변경 불가)
  - member: 조회 + 생성만 (수정/삭제 불가)

### 3-5. 워크스페이스 전환
- [ ] 사이드바 상단에 워크스페이스 전환 드롭다운
- [ ] 여러 워크스페이스에 소속된 경우 전환 가능
- [ ] 전환 시 데이터 리로드

### 3-6. 활동 로그 (선택사항, 시간 남으면)
- [ ] `activity_logs` 테이블 (who, what, when)
- [ ] 청구서 생성/수정/상태변경 시 자동 기록
- [ ] 대시보드 또는 설정에서 로그 조회

---

## 작업 순서 권장

```
1. 환경 설정 마무리 (Supabase Redirect URL)
2. 카테고리 SQL 실행 확인
3. 초대 DB 스키마 + 초대 링크 생성/수락
4. 멤버 관리 페이지
5. 역할 권한 분기
6. 워크스페이스 전환
7. (선택) 활동 로그
8. 빌드 → 커밋 → 배포
```

---

## 참고 명령어

```bash
# 로컬 개발
cd C:/Users/aimej/Downloads/MyInvoice
npm run dev

# 빌드 확인
npm run build

# 커밋 & 푸시
git add . && git commit -m "feat: ..." && git push

# Vercel 배포
npx vercel deploy --prod
```
